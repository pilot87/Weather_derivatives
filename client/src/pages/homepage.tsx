import React from 'react'export const Homepage = ({auth}: any) => {    if (auth.name === '') {        window.location.assign('futures')    } else {        window.location.assign('about')    }    return (        <nav>            <div className="nav-wrapper">                <div className="col s12">                    <p className="breadcrumb">Home</p>                </div>            </div>        </nav>    )}